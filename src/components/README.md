# components

Shared Svelte components used across multiple feature tabs (charts, cards, etc.) — see the "Project Structure" section of the top-level README.md. Route-specific components live alongside their route under `src/routes/`.

## ui/

Base UI components from shadcn-svelte + custom implementations, styled with Tailwind CSS. All components are unstyled by default and composed together to build feature-specific layouts.
